apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "monorepo-chart.fullname" . }}-test-connection"
  labels:
    {{- include "monorepo-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "monorepo-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
