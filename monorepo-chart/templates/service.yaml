apiVersion: v1
kind: Service
metadata:
  name: {{ include "monorepo-chart.fullname" . }}
  labels:
    {{- include "monorepo-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "monorepo-chart.selectorLabels" . | nindent 4 }}
